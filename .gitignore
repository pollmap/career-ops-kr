# === 유저 레이어 (개인정보 — public repo 제외) ===
# CLAUDE.md 데이터 계약: User 레이어는 찬희 개인 커스터마이징.
# public 배포 시 이력서/개인 프로필은 포함하지 않고, 템플릿(`templates/profile.example.yml`,
# `modes/_profile.template.md`)만 공개한다. 새 사용자는 `career-ops init` 으로 생성.
cv.md                     # 개인 이력서 — 실명/학적/연락처 포함 가능
config/profile.yml        # 개인 프로필 — 타겟 산업/휴학 상태/스코어 선호 등
modes/_profile.md         # 개인 narrative / archetype / 협상 스크립트

# === 유저 데이터 / 런타임 산출물 ===
data/
reports/
output/
.auth/                    # Playwright storage state (로그인 세션)
*.db
*.db-journal

# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# === 가상환경 / uv ===
.venv/
venv/
ENV/
env/
.python-version
# uv lockfile — 팀 결정에 따라 포함 여부 결정. 당분간 제외.
uv.lock

# === 테스트 / 커버리지 ===
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
.hypothesis/

# === 타입 체크 ===
.mypy_cache/
.dmypy.json
dmypy.json
.ruff_cache/

# === IDE ===
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# === Playwright ===
playwright-report/
test-results/
.cache/

# === 시크릿 (절대 커밋 금지) ===
.env
.env.local
.env.*.local
secrets.yml
*.secret

# === 로그 ===
*.log
logs/

# === 플랜/메모 (로컬 전용) ===
scratch/
notes/local/

# Audit 2026-04-20 — internal sprint memos
SPRINT*.md
*_PROMPT.md
